#include <iostream>
#include <list>
#include <vector>
#include <string>
#include "servidor.h"
#include "usuario.h"


int main() {
  
  // System system;
  // executor executor(system);

  // system.load();

  
  executor.start(cin, cout);

  return 0;
}